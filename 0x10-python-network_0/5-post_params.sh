#!/bin/bash
# takes in a URL, sends a POST request to that URL, and displays the size of the body of the response
curl -sd -X "POST" "subject=I will always be here for PLD&email=hr@holbertonschool.com"
